# ╔══════════════════════════════════════════════════════════════╗
# ║  STOA LINUX — Neofetch                                      ║
# ║  "Know thyself."                                              ║
# ╚══════════════════════════════════════════════════════════════╝

print_info() {
    info title
    info underline

    info "Virtue" distro
    info "Logos" kernel
    info "Uptime" uptime
    info "Packages" packages
    info "Agora" shell
    info "Column" wm
    info "Theme" theme
    info "Icons" icons
    info "Font" term_font
    info "Mind" cpu
    info "Memory" memory
    info "Disk" disk

    info cols
}

# ── Appearance ──
title_fqdn="off"
kernel_shorthand="on"
distro_shorthand="off"
os_arch="on"
uptime_shorthand="on"
memory_percent="on"
package_managers="on"
shell_path="off"
shell_version="on"
speed_type="bios_limit"
speed_shorthand="on"
cpu_brand="on"
cpu_speed="on"
cpu_cores="logical"
cpu_temp="C"
gpu_brand="on"
gpu_type="all"
disk_show=('/')
disk_subtitle="mount"

# ── Colors ──
colors=(3 7 3 3 7 7)
bold="on"
underline_enabled="on"
underline_char="─"
separator=" ›"

# ── Backend ──
image_backend="ascii"
image_source="auto"

# ── Custom ASCII ──
ascii_distro="auto"
ascii_colors=(3 7)
ascii_bold="on"

# ── Output ──
gap=3
