__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
README.md
LICENSE
scripts/stoa-dfm/dfm.egg-info/

# Hyprland's autogenerated fallback config. Stoa ships hyprland.lua; the
# .conf path only ever reappears when Hyprland starts without a config
# and writes its own stub. When ~/.config/hypr/hyprland.conf is still a
# symlink from a pre-lua install, that write lands *through* the dangling
# link and recreates the file in here, where `stoa-sync`'s `git add -A`
# would happily commit it. Ignore it so the stub can never come back.
config/hypr/hyprland.conf

# Config backups. stoa-maintain writes "<host>.confs.<date>.zip" and the
# restore paths write "pre_restore_<stamp>.zip"; both used to be relative,
# so running a backup from this checkout dropped a multi-megabyte archive
# in here that `stoa-sync`'s `git add -A` then committed. The paths are
# anchored to $HOME now — this is the belt to that pair of braces.
*.confs.*.zip
pre_restore_*.zip
